{
  "name": "ylonz16",
  "version": "0.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "publish": "aws --profile personal s3 sync build s3://asaservice.space/",
    "server": "node node_modules/live-server/live-server.js",
    "build": "mkdir -p build/ && cp -r index.html img/ build/ && node_modules/node-sass/bin/node-sass --output-style compressed styles.scss > build/styles.css",
    "sass": "node_modules/node-sass/bin/node-sass -w styles.scss -o ."
  },
  "author": "Oskar Ehnström",
  "license": "MIT",
  "devDependencies": {
    "live-server": "0.9.2",
    "node-sass": "3.4.2"
  }
}
